import React, { useState, useEffect, useCallback, useMemo } from 'react';
import styled from 'styled-components';

const OkxConnect = () => {
    return (
        <>
            <div>
                <button>Connect to OKX Wallet</button>
            </div>
        </>
    );
}

export default OkxConnect;